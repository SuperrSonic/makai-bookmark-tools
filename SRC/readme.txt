"conv_script.cmd" can be used to drag-n-drop a script to decompress it.

"MnB_table_ref.txt" contains the table in readable form, it is unused, as everything is done internally in the program. It also contains notes for possible errors.
